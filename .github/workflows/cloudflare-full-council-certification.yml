name: Full Cloudflare Council Member Certification

on:
  pull_request:
    branches: [cpl-quality-noise-governor]
    paths:
      - '.github/workflows/cloudflare-full-council-certification.yml'
      - 'main_review/cloudflare_cli.py'
      - 'main_review/cloudflare_models.py'
      - 'main_review/cloudflare_scout_qualification.py'
      - 'main_review/llm_provider.py'
      - 'tests/test_cloudflare_full_roster_qualification.py'

permissions:
  contents: read

concurrency:
  group: cloudflare-full-council-certification-${{ github.ref }}
  cancel-in-progress: false

jobs:
  certify-all-members:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    env:
      SERGEANT_CLOUDFLARE_ACCOUNT_ID: ${{ secrets.SERGEANT_CLOUDFLARE_ACCOUNT_ID }}
      SERGEANT_CLOUDFLARE_API_TOKEN: ${{ secrets.SERGEANT_CLOUDFLARE_API_TOKEN }}
      SERGEANT_CLOUDFLARE_MODELS: '@cf/zai-org/glm-4.7-flash,@cf/qwen/qwen2.5-coder-32b-instruct,@cf/ibm-granite/granite-4.0-h-micro,@cf/openai/gpt-oss-120b,@cf/moonshotai/kimi-k2.7-code,@cf/qwen/qwen3-30b-a3b-fp8,@cf/openai/gpt-oss-20b'
      SERGEANT_CPL_ENABLED: 'true'
      SERGEANT_CPL_POLICY: required
      SERGEANT_CPL_DEPTH: maximum
      SERGEANT_CPL_MAX_PASSES: '7'
      SERGEANT_CPL_MAX_COUNCIL_MEMBERS: '7'
      SERGEANT_CPL_MAX_ROUNDS: '3'
      SERGEANT_CPL_MAX_INPUT_CHARS: '16000'
      SERGEANT_CLOUDFLARE_TIMEOUT_SECONDS: '90'
    steps:
      - name: Checkout exact candidate head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Sergeant and focused test dependency
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest

      - name: Run focused compatibility tests
        run: python -m pytest -q tests/test_cloudflare_full_roster_qualification.py tests/test_cloudflare_mission_qualification.py

      - name: Record candidate head
        run: |
          mkdir -p build
          git rev-parse HEAD | tee build/full-council-tested-sha.txt

      - name: Create role-specific mission fixtures
        run: |
          mkdir -p build/full-council-fixture/src
          cat > build/full-council-fixture/src/auth.py <<'PY'
          import subprocess

          def run_user_command(request):
              command = request.args.get("command")
              return subprocess.run(command, shell=True)
          PY
          cat > build/full-council-fixture/src/scout.py <<'PY'
          RETRY_LIMIT = 3
          TIMEOUT_SECONDS = 15
          SUPPORTED_LANGUAGES = ["python", "javascript"]
          PY

      - name: Capture credential-safe route status
        run: sergeant-cloudflare --pretty status --require > build/full-council-status.json

      - name: Prove structured transport for all seven members
        id: handshake
        shell: bash
        run: |
          set +e
          sergeant-cloudflare --pretty test-models --require > build/full-council-handshake.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Prove officer missions for six reasoning members
        id: officer
        env:
          SERGEANT_CLOUDFLARE_MODELS: '@cf/zai-org/glm-4.7-flash,@cf/qwen/qwen2.5-coder-32b-instruct,@cf/openai/gpt-oss-120b,@cf/moonshotai/kimi-k2.7-code,@cf/qwen/qwen3-30b-a3b-fp8,@cf/openai/gpt-oss-20b'
        shell: bash
        run: |
          set +e
          sergeant-cloudflare --pretty qualify-models build/full-council-fixture \
            --files src/auth.py \
            --expected-verdict BLOCK \
            --expected-path src/auth.py \
            --expected-category security \
            --expected-severity blocker \
            --expected-evidence shell=True \
            --minimum-models 6 \
            --require > build/full-council-officer-mission.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Prove Granite Scout evidence extraction mission
        id: scout
        env:
          SERGEANT_CLOUDFLARE_MODELS: '@cf/ibm-granite/granite-4.0-h-micro'
        shell: bash
        run: |
          set +e
          python -m main_review.cloudflare_scout_qualification build/full-council-fixture \
            --file src/scout.py --pretty --require > build/full-council-scout-mission.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Build member-by-member certification summary
        if: always()
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          expected = [
              '@cf/zai-org/glm-4.7-flash',
              '@cf/qwen/qwen2.5-coder-32b-instruct',
              '@cf/ibm-granite/granite-4.0-h-micro',
              '@cf/openai/gpt-oss-120b',
              '@cf/moonshotai/kimi-k2.7-code',
              '@cf/qwen/qwen3-30b-a3b-fp8',
              '@cf/openai/gpt-oss-20b',
          ]

          def load(path):
              file = Path(path)
              try:
                  return json.loads(file.read_text(encoding='utf-8'))
              except Exception:
                  return {}

          status = load('build/full-council-status.json')
          handshake = load('build/full-council-handshake.json')
          officer = load('build/full-council-officer-mission.json')
          scout = load('build/full-council-scout-mission.json')
          handshake_rows = {
              str(row.get('model')): row
              for row in handshake.get('models', [])
              if isinstance(row, dict) and row.get('model')
          }
          mission_rows = {
              str(row.get('model')): row
              for row in officer.get('models', [])
              if isinstance(row, dict) and row.get('model')
          }
          mission_rows.update({
              str(row.get('model')): row
              for row in scout.get('models', [])
              if isinstance(row, dict) and row.get('model')
          })
          members = []
          for model in expected:
              transport = handshake_rows.get(model, {})
              mission = mission_rows.get(model, {})
              members.append({
                  'model': model,
                  'structured_transport_passed': transport.get('passed') is True,
                  'role_mission_passed': mission.get('passed') is True,
                  'transport_error': transport.get('error'),
                  'mission_error': mission.get('error'),
                  'mission_response': mission.get('response'),
              })
          certified = [
              item['model']
              for item in members
              if item['structured_transport_passed'] and item['role_mission_passed']
          ]
          payload = {
              'schema_version': 'sergeant.cloudflare-full-council-certification.v2',
              'tested_sha': Path('build/full-council-tested-sha.txt').read_text(encoding='utf-8').strip(),
              'expected_member_count': len(expected),
              'structured_pass_count': handshake.get('passed_count', 0),
              'officer_mission_pass_count': officer.get('passed_count', 0),
              'scout_mission_pass_count': scout.get('passed_count', 0),
              'certified_member_count': len(certified),
              'certified_models': certified,
              'uncertified_models': [model for model in expected if model not in certified],
              'route_valid': status.get('valid') is True,
              'members': members,
              'handshake_exit': int('${{ steps.handshake.outputs.exit_code }}' or '1'),
              'officer_exit': int('${{ steps.officer.outputs.exit_code }}' or '1'),
              'scout_exit': int('${{ steps.scout.outputs.exit_code }}' or '1'),
          }
          payload['passed'] = bool(
              payload['route_valid']
              and payload['structured_pass_count'] == 7
              and payload['officer_mission_pass_count'] == 6
              and payload['scout_mission_pass_count'] == 1
              and payload['certified_member_count'] == 7
          )
          Path('build/full-council-certification-summary.json').write_text(
              json.dumps(payload, indent=2, sort_keys=True) + '\n',
              encoding='utf-8',
          )
          print(json.dumps(payload, indent=2, sort_keys=True))
          PY

      - name: Verify credentials are absent from artifacts
        id: credential_scan
        if: always()
        run: |
          python - <<'PY'
          import os
          from pathlib import Path

          values = [
              os.getenv('SERGEANT_CLOUDFLARE_ACCOUNT_ID', ''),
              os.getenv('SERGEANT_CLOUDFLARE_API_TOKEN', ''),
          ]
          artifacts = list(Path('build').glob('full-council-*.json'))
          assert artifacts
          for path in artifacts:
              text = path.read_text(encoding='utf-8', errors='replace')
              for value in values:
                  if value:
                      assert value not in text, f'Credential or account identifier leaked into {path}'
          PY

      - name: Upload full council certification evidence
        if: always() && steps.credential_scan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: cloudflare-full-council-certification
          path: |
            build/full-council-tested-sha.txt
            build/full-council-status.json
            build/full-council-handshake.json
            build/full-council-officer-mission.json
            build/full-council-scout-mission.json
            build/full-council-certification-summary.json
          if-no-files-found: error
          retention-days: 14

      - name: Enforce all-member certification
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          payload = json.loads(Path('build/full-council-certification-summary.json').read_text(encoding='utf-8'))
          assert payload.get('passed') is True, 'Not all seven Cloudflare council members certified.'
          PY
