{
  "schema_version": "sergeant.blind-benchmark.case.v1",
  "id": "rust-contained-path",
  "title": "Rust request path is canonicalized and contained",
  "expected_verdict": "APPROVE",
  "changed_files": ["src/files.rs", "tests/files.rs"],
  "expected_findings": [],
  "files": [
    {"path": "Cargo.toml", "content": "[package]\nname = \"files-service\"\nversion = \"0.1.0\"\nedition = \"2021\"\n"},
    {"path": "README.md", "content": "# Rust files service\n"},
    {"path": ".github/workflows/ci.yml", "content": "name: ci\non: [push]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - run: cargo test\n"},
    {"path": "src/files.rs", "content": "use std::fs;\nuse std::path::Path;\n\npub fn download(requested: &str) -> std::io::Result<Vec<u8>> {\n    let root = Path::new(\"/srv/files\").canonicalize()?;\n    let candidate = root.join(requested).canonicalize()?;\n    candidate.strip_prefix(&root).map_err(|_| std::io::ErrorKind::PermissionDenied)?;\n    fs::read(candidate)\n}\n"},
    {"path": "tests/files.rs", "content": "#[test]\nfn download_symbol_exists() { let _ = files_service::download; }\n"}
  ]
}
