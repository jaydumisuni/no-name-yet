# Generate unique random values before starting the service.
# Example generator:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Required by compose.yaml. Do not leave this blank and do not reuse another token.
SERGEANT_SERVICE_TOKEN=

# Optional. Set a different random value only when GitHub webhook intake is used.
SERGEANT_WEBHOOK_SECRET=
