# Generate unique random values before starting the service.
# Example generator:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

SERGEANT_SERVICE_TOKEN=replace-with-a-random-value-at-least-24-characters

# Optional. Required only when GitHub webhook intake is used.
SERGEANT_WEBHOOK_SECRET=
